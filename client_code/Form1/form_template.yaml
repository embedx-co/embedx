components: []
container:
  properties:
    html: |
      <!DOCTYPE html>
      <html>
      <head>
        <style>
          body {
            background-color: black;
            color: white; /* Optional: Makes default text easier to read */
            font-family: Arial, sans-serif;
          }
          .centered-text {
            font-style: italic;
            color: #888;
            margin: 3em;
            text-align: center;
          }
        </style>
      </head>
      <body>
        <div class="centered-text">
          (Insert your custom HTML here)
        </div>
        <div anvil-slot="default"></div>
      </body>
      </html>
  type: HtmlTemplate
is_package: true
